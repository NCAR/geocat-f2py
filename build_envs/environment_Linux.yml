name: geocat_f2py_build
channels:
  - conda-forge
  - ncar
dependencies:
  - python
  - dask
  - gfortran_linux-64
  - liblapack
  - numpy=1.19
  - pip
  - pytest  # for tests
  - pytest-cov  # for tests
  - xarray
  - pip:
      - pre-commit
